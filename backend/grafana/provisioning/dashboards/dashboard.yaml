apiVersion: 1
providers:
  - name: hackstrom
    folder: HackStrom26
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards
  - name: trustflow-ops
    folder: TrustFlow
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards
